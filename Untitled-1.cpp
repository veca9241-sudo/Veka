#include <iostream>
using namespace std;
void main()
{
    int a;
    cin >> a;
    if (a % 2 == 0)
        &&(a >= 10 && a = < 99) if (a == true)
                cout
            << "Suma je:" << sqrt(a) + pow(a, 2);
    system("pause>0");
}
}