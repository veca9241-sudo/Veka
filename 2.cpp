#include <iostream>
#include <cmath>
using namespace std;
void main()
{
    float a, b;
    cin >> a >> b;
    if (a == true && b == true)
        cout << "Unesite nove vrijednosti: " << a << " i " << b << endl;
    int suma = a + b;
    else(a > 0 || b > 0)
    {
        if (a > 0))
                cout << "Kub broja a je: " << pow(a, 3) << endl;
        if (b <= 0)
            cout << "Kub broja b je: " << sqrt(b) << endl;

        system("pause>0");
    }
}